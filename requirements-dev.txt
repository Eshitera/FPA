# Developer / admin tools NOT needed in production deploys.
# Install locally with:  python -m pip install -r requirements-dev.txt

-r requirements.txt

# Conda/packaging admin tools (keep out of production image)
conda-repo-cli==1.0.75
clyent==1.2.1

# Linters / IDE helpers (optional)
black
flake8
isort
mypy
pytest
